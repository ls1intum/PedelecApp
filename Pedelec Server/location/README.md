# Location Microservice

The service is available at [http://localhost:8081](http://localhost:8081).

### Docker Image
Build the docker image using the following command:
```bash
docker build -t location .
```