# Damage Microservice

The service is available at [http://localhost:8082](http://localhost:8082).

### Docker Image
Build the docker image using the following command:
```bash
docker build -t damage .
```